# kiro-kit の環境依存設定。~/.kiro/kit.env として置く。
# install.sh が雛形を作るので、必要なところだけ書き換える。

# 作業ログの置き場所。設定するとセッション開始時に直近5件が自動で表示され、
# start-work / save-log / recall skill がこの場所を使う。
# 使わないなら空のままでよい(その機能だけ黙ってスキップされる)。
KIRO_WORK_LOG_DIR=""
